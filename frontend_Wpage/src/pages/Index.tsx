import { MeasureDisplay } from "@/components/MeasureDisplay";

const Index = () => {
  return (
    <div className="min-h-screen w-full bg-gradient-to-b from-gray-900 to-gray-800 flex items-center justify-center p-4">
      <MeasureDisplay />
    </div>
  );
};

export default Index;
